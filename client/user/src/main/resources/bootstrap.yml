server:
  port: 8340

spring:
  application:
    name: news-client-user  #和SpringCloudConig的配置文件前缀一样
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8000/
      label: master
      profile: dev


