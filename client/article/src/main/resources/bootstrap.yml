server:
  port: 8310

spring:
  application:
    name: news-client-article  #和SpringCloudConig的配置文件前缀一样
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8000/
      label: master
      profile: dev
      name: ${spring.application.name},common



